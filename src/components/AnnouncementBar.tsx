const AnnouncementBar = () => (
  <div className="bg-espresso text-primary-foreground text-center py-2.5 px-4">
    <p className="text-xs tracking-[0.2em] uppercase font-body font-light">
      Free shipping on orders over $75 · Cotton, in every tone
    </p>
  </div>
);

export default AnnouncementBar;
